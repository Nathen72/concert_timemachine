# Spotify API Configuration
#
# To set up Spotify API:
# 1. Go to https://developer.spotify.com/dashboard
# 2. Log in with your Spotify account
# 3. Click "Create an App"
# 4. Fill in the app name and description
# 5. Copy the Client ID and paste it below
# 6. In the app settings, click "Edit Settings"
# 7. Add the following redirect URIs:
#    - http://127.0.0.1:5173/callback (for development)
#    - http://localhost:5173/callback (also add this for compatibility)
#    - https://your-production-url.vercel.app/callback (for production)
# 8. Save the settings
# 9. Copy this file to .env and fill in your Client ID
# 10. Access the app at http://127.0.0.1:5173 (not localhost)
#
# NOTE: You need Spotify Premium for playback to work!
# NOTE: Use 127.0.0.1 instead of localhost to avoid "Insecure redirect URI" errors

VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
VITE_REDIRECT_URI=http://127.0.0.1:5173/callback
